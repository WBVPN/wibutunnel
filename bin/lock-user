#!/bin/bash
# ==========================================
# WIBU TUNNELING - lock-user (Improved v2)
# [PATCH] flock via safe_jq_edit_args, safe_sed_delete
# ==========================================

source /usr/local/bin/common.sh 2>/dev/null || { echo "[ERROR] common.sh not found" >&2; exit 1; }
load_bot_conf

user=$1
duration=$2
reason=$3   # IP_LIMIT atau QUOTA

[[ -z "$user" ]] && exit 1

if ! safe_jq_edit_args --arg u "$user" '(.routing.rules[] | select(.user != null and .outboundTag == "blocked") | .user) |= (. + [$u] | unique)'; then
    echo "[ERROR] lock-user: config xray tidak bisa diedit, $user tidak dikunci" >&2
    exit 1
fi

now=$(date +%s)
if [[ -n "$duration" && "$duration" != "0" ]]; then
    unlock_time=$((now + (duration * 60)))
else
    unlock_time=0
fi

safe_sed_delete "$user" /etc/wibutunnel/locked_users.db
echo "$user:$now:$unlock_time:$reason" >> /etc/wibutunnel/locked_users.db

systemctl restart xray >/dev/null 2>&1

# === TELEGRAM NOTIFICATION ===
if [[ -n "$BOT_TOKEN" && -n "$CHAT_ID" ]]; then
    DOMAIN=$(cat /etc/xray/domain 2>/dev/null || echo "Unknown")
    WAKTU=$(date '+%d %b %Y %H:%M WIB')

    if [[ "$reason" == "IP_LIMIT" ]]; then
        ALASAN="Melebihi Limit IP"
    elif [[ "$reason" == "QUOTA" ]]; then
        ALASAN="Kuota Habis"
    else
        ALASAN="Manual Lock"
    fi

    if [[ "$duration" == "0" ]]; then
        DURASI_TXT="Permanen (sampai di-unlock manual)"
    else
        DURASI_TXT="${duration} menit"
    fi

    PESAN="
🔒 <b>USER DIKUNCI OTOMATIS</b>

<b>User     :</b> <code>${user}</code>
<b>Alasan   :</b> ${ALASAN}
<b>Durasi   :</b> ${DURASI_TXT}
<b>Waktu    :</b> ${WAKTU}
<b>Domain   :</b> <code>${DOMAIN}</code>

<i>Account akan otomatis dibuka setelah durasi habis.</i>
"

    tg_curl -X POST sendMessage \
        -F "chat_id=${CHAT_ID}" \
        -F "disable_web_page_preview=true" \
        -F "parse_mode=html" \
        -F "text=${PESAN}" >/dev/null 2>&1
fi
